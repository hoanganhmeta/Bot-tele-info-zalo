[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

# Tạm thời tắt tất cả redirect để test
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
